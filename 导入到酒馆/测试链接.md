# 测试脚本链接是否可用

## 当前使用的链接（版本标签）

```
https://cdn.jsdelivr.net/gh/onlinedone/Cyber_prisoner_simulator@v1.0.0/dist/赛博坐牢模拟器增强脚本/index.js
```

## 测试方法

### 方法 1：浏览器直接访问

在浏览器中打开以下链接，如果能看到 JavaScript 代码，说明链接可用：

**版本标签链接**：
https://cdn.jsdelivr.net/gh/onlinedone/Cyber_prisoner_simulator@v1.0.0/dist/赛博坐牢模拟器增强脚本/index.js

**提交哈希链接**（备选）：
https://cdn.jsdelivr.net/gh/onlinedone/Cyber_prisoner_simulator@e5c84654a91e1c7793ff221e3b21d92a0a60b317/dist/赛博坐牢模拟器增强脚本/index.js

**GitHub Raw 链接**（备选）：
https://raw.githubusercontent.com/onlinedone/Cyber_prisoner_simulator/main/dist/赛博坐牢模拟器增强脚本/index.js

### 方法 2：浏览器控制台测试

1. 按 `F12` 打开开发者工具
2. 切换到 **Console** 标签
3. 输入以下代码：

```javascript
// 测试版本标签链接
fetch('https://cdn.jsdelivr.net/gh/onlinedone/Cyber_prisoner_simulator@v1.0.0/dist/赛博坐牢模拟器增强脚本/index.js')
  .then(response => {
    if (response.ok) {
      console.log('✅ 版本标签链接可用');
      return response.text();
    } else {
      console.error('❌ 版本标签链接不可用:', response.status);
    }
  })
  .then(text => {
    if (text) {
      console.log('文件大小:', text.length, '字符');
      console.log('文件开头:', text.substring(0, 100));
    }
  })
  .catch(error => {
    console.error('❌ 请求失败:', error);
  });
```

### 方法 3：在 SillyTavern 中测试

1. 导入脚本 JSON 文件
2. 启用脚本
3. 打开浏览器控制台（F12）
4. 查看是否有以下日志：
   - `[核心系统] 酒馆页面已加载`
   - `[状态栏] 脚本加载完成 v3.5.2`
   - `[事件系统] 脚本加载完成 v3.4.0`
   - `[看守所模拟器] 所有模块已加载完成`

## 如果版本标签链接无法访问

### 可能的原因

1. **jsdelivr 缓存延迟**：新创建的 Release 可能需要 5-10 分钟才能被 jsdelivr 识别
2. **网络问题**：检查网络连接
3. **CDN 同步延迟**：等待一段时间后重试

### 解决方案

1. **等待 5-10 分钟后重试**
2. **使用提交哈希链接**（更可靠）：
   ```javascript
   import 'https://cdn.jsdelivr.net/gh/onlinedone/Cyber_prisoner_simulator@e5c84654a91e1c7793ff221e3b21d92a0a60b317/dist/赛博坐牢模拟器增强脚本/index.js'
   ```
3. **使用 GitHub Raw 链接**（最可靠）：
   ```javascript
   import 'https://raw.githubusercontent.com/onlinedone/Cyber_prisoner_simulator/main/dist/赛博坐牢模拟器增强脚本/index.js'
   ```

## 当前状态

✅ 仓库已公开
✅ Release v1.0.0 已创建
✅ 版本标签链接应该可以使用

如果链接仍然无法访问，请等待 5-10 分钟后重试，或使用备选链接。
